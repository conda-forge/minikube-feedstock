context:
  name: minikube
  version: "1.38.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  - url: https://github.com/kubernetes/${{ name }}/archive/refs/tags/v${{ version }}.tar.gz
    sha256: 0938a1839bd5c5cb78eac2ff7da931dbbc062d83437351906e4d8d2b53b398c6

build:
  number: 0

requirements:
  build:
    - if: linux and x86_64
      then:
        - ${{ compiler('go-cgo') }}
        - ${{ compiler('c') }}
      else:
        - ${{ compiler('go-nocgo') }}
    - go-licenses
    - make
    - if: win
      then:
        - git
        - posix
  run:
    - kubernetes

tests:
  - script:
      - minikube version

about:
  homepage: http://github.com/kubernetes/minikube
  license: Apache-2.0
  license_file:
    - LICENSE
    - license-files/
  summary: Run Kubernetes locally
  description: |
    minikube implements a local Kubernetes cluster.
    minikube's primary goals are to be the best tool for local Kubernetes
    application development and to support all Kubernetes features that fit.
  documentation: https://minikube.sigs.k8s.io/docs/
  repository: https://github.com/kubernetes/minikube

extra:
  recipe-maintainers:
    - xmnlab
    - esloch
    - delsner
